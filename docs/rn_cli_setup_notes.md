# React Native CLI Setup Notes (No Expo)

1. Initialize app with React Native CLI TypeScript template.
2. iOS: run `pod install` in `ios/`.
3. Configure APNs push credentials.
4. Add keychain secure token storage.
5. Add deep link support (iOS universal links).
6. Configure release signing and CI lanes.
